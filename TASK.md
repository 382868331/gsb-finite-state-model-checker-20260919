# 完整首轮任务

请从零实现“有限并发系统反例生成库”，用于形式化方法 / 状态空间探索中的本地可复用模块。

1. 从零实现有限系统模型检查库，状态为具名有界整数变量元组，初始状态唯一。迁移有唯一ID、守卫及同时赋值表达式；自定义小表达式AST支持常量、变量、整数加减、比较和布尔运算，禁止eval和任意回调执行。
2. 同一迁移所有右值从旧状态计算，超出变量域使该迁移禁用；枚举全部可达状态，迁移按ID排序。检查状态安全谓词及死锁，死锁为无可执行迁移，可指定终止谓词豁免正常结束。
3. 安全失败返回迁移条数最短、ID序列字典序最小的反例及逐步状态；实现独立重放验证器检查每一步守卫、同时赋值与最终失败，不能只输出文字解释。
4. 支持活性性质“每条无限执行都无限次访问accept谓词状态”，不假设公平性。通过非accept子图的可达有环SCC寻找反例，返回从初始态可重放的前缀+非空闭环；自环算环，有限终止不算该活性违例。
5. 状态/边预算可配置，超限返回unknown及已探索计数，不能声称通过；只有充分证据时返回fail，完整探索后才可pass。仅有限模型内核，示例为两个进程互斥与饥饿小模型，不做UI或分布式平台。

验收重点：同时赋值交换、守卫禁用、短反例平局、自环饥饿、只有有限终止、SCC含accept但存在非accept子环、预算截断。

工程范围：这是全新核心库的0-1实现，初始仓库仅有运行骨架，没有业务实现。仅使用Python 3.14.7标准库，在Windows原生环境离线运行；无需安装第三方依赖。不接数据库、中间件、云API、外部程序，不使用Docker，不制作前后端或通用CLI。
交付：实现可复用库、聚焦上述语义的自动化测试和README；python demo.py须在约8秒内运行确定性小案例，打印关键正常结果和至少一种真实边界失败，便于录制十几秒视频。测试命令为python -m unittest discover -s tests -v。先阅读本仓库README和TASK.md，简短计划后集中实现，再运行针对性测试与演示；避免反复通读仓库、无关探索和无必要的长篇文档，发现实际问题时正常修复验证，不设硬性工具调用截断。
只能操作当前仓库内文件；不读取用户配置、密钥、环境变量清单或其他工作区。不要新增GitHub Actions、CI、部署、Dependabot或远程推送。一次首轮完成，不要求用户追加“继续”；合理实现取舍写入README。不要为通过测试删除需求、硬编码示例输出或宣称未实际执行的验证通过。

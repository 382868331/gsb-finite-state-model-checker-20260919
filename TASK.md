# 完整首轮任务

并发协议设计者希望在小型有界模型上发现安全违例和饥饿反例。请从零实现“有限并发系统反例生成库”。

1. 实现有限模型检查库，状态为<=6个具名有界整数变量，初态唯一，<=20条唯一ID迁移；守卫/同时赋值使用小AST，支持整数常量/变量/加减/比较与布尔运算。预检类型、未知变量和AST深度<=32，禁止eval或任意回调。
2. 所有赋值右值读取旧状态，结果越界使迁移禁用；按ID字典序枚举可达迁移。检查安全谓词和死锁，无启用迁移且不满足终止谓词才算死锁。
3. 安全/死锁失败返回最少迁移数、ID序列字典序最小的反例和逐步状态；独立重放检查守卫、同时赋值、终点违例，允许初态即失败的空路径。
4. 检查每条无限执行都无限次访问accept，不假设公平性：先求完整迁移图可达状态，再在这些状态中取非accept诱导子图找有环SCC。返回从初态出发的前缀和非空闭环，前缀可以经过accept；有限终止不是此活性违例。
5. 配置状态/边预算，默认各10000/50000，超限返回unknown及计数；发现可重放反例可fail，完整探索后才pass。示例模型<=100状态，不支持完整LTL、偏序规约或GUI。

验收重点：同时赋值、越界禁用、初态失败、短反例平局、自环、经accept前缀到坏环、有限终止、预算unknown。

工程约束：全新0-1核心库，仅用Python 3.14.7标准库，在Windows原生离线运行，无第三方依赖、外部服务、Docker或前后端。交付实现、聚焦上述边界的测试及简短README；演示命令`python demo.py`用小案例在约8秒内打印正常结果和一种真实失败，测试命令`python -m unittest discover -s tests -v`。固定种子随机验证只用少量小样本，避免长压测和额外平台功能，不限制正常纠错所需工具调用。
一次首轮完成，不要求用户追加“继续”。只操作当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区；不添加Actions/CI/部署/Dependabot、不远程推送，不硬编码演示或虚报验证。

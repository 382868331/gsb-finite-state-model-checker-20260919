# 完整首轮任务

请实现一个有限状态模型检查库，用小型并发模型查找安全违例、死锁和活性反例。反例要能逐步重放；没有完成探索时，需要明确返回未知。

实现有限模型检查库，状态为<=6个具名有界整数变量，初态唯一，<=20条唯一ID迁移；守卫/同时赋值使用小AST，支持整数常量/变量/加减/比较与布尔运算。预检类型、未知变量和AST深度<=32，禁止eval或任意回调。

所有赋值右值读取旧状态，结果越界使迁移禁用；按ID字典序枚举可达迁移。检查安全谓词和死锁，无启用迁移且不满足终止谓词才算死锁。

安全/死锁失败返回最少迁移数、ID序列字典序最小的反例和逐步状态；独立重放检查守卫、同时赋值、终点违例，允许初态即失败的空路径。

检查每条无限执行都无限次访问accept，不假设公平性：先求完整迁移图可达状态，再在这些状态中取非accept诱导子图找有环SCC。返回从初态出发的前缀和非空闭环，前缀可以经过accept；有限终止不是此活性违例。

配置状态/边预算，默认各10000/50000，超限返回unknown及计数；发现可重放反例可fail，完整探索后才pass。示例模型<=100状态，不支持完整LTL、偏序规约或GUI。

测试里请覆盖这些情况：同时赋值、越界禁用、初态失败、短反例平局、自环、经accept前缀到坏环、有限终止、预算unknown。

使用 Python 3.14.7 标准库，代码要能在 Windows 原生环境离线运行。范围就是这个库，不需要第三方依赖、外部服务、Docker 或前后端。请一并写好测试和简短的 README，说明接口及运行方法。

用 `python demo.py` 运行演示，在约 8 秒内展示一个正常结果和一个实际触发的失败；用 `python -m unittest discover -s tests -v` 运行测试。随机验证采用固定种子和少量小样本即可，不做长时间压力测试，也不扩展额外的平台功能。

请在本轮完成实现和验证，不要等我回复“继续”。正常排错需要的工具调用不设次数限制。操作范围限于当前仓库，不读取用户配置、密钥、环境变量清单或其他工作区。不要添加 Actions、CI、部署或 Dependabot，也不要向远端推送。演示结果应由代码实际计算；最后如实说明验证结果，未运行或未通过的检查也请写明。
